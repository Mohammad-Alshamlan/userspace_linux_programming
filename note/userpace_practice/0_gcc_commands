//

// for debugging
gcc questio2.c -o questio2 -g

// for pthread
gcc str_ex1_v1.c -o str_ex1_v1 -lpthread

// to look at every library location and preprocess of gcc
gcc -v -E str_ex1_v1.c -o str_ex1_v1

gcc -v -E measure_type_size.c -o measure_type_size 

// to look the code of library use this
	echo "#include<limits.h>" | gcc -E - | gedit
eqvulaent to
	echo "#include<limits.h>" | cpp | gedit


